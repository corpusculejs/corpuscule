const packages = {
  context: ['index'],
  element: ['index'],
  redux: ['index'],
  router: ['index'],
  styles: ['index'],
  utils: [
    'addToRegistry',
    'assertKind',
    'descriptors',
    'getSuperMethod',
    'compose',
  ],
};

const definitions = ['typings'];

module.exports = {
  definitions,
  packages,
};
