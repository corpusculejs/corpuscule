language: node_js
dist: trusty
addons:
  chrome: stable
node_js:
  - '10'
cache:
  directories:
  - $HOME/.npm
script: npm test
after_success:
  - ./node_modules/.bin/codecov -e TRAVIS_NODE_VERSION -f .coverage/lcov.info
