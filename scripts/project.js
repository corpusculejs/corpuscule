const packages = {
  context: ["index"],
  element: ["index"],
  redux: ["index"],
  router: ["index"],
  styles: ["index"],
  utils: [
    "pipe",
    "propertyChain",
  ],
};

const definitions = ["types"];

module.exports = {
  definitions,
  packages,
};
